diff --git a/backend/app/providers/base.py b/backend/app/providers/base.py
--- a/backend/app/providers/base.py
+++ b/backend/app/providers/base.py
@@ -1,7 +1,7 @@
 """Contrato mínimo que deben cumplir los proveedores de triaje."""
 
 from collections.abc import Mapping
-from dataclasses import dataclass
+from dataclasses import dataclass, field
 from typing import Protocol
@@ -14,6 +14,9 @@ class ToolCall:
 
     name: str
     arguments: Mapping[str, object]
+    provider_context: Mapping[str, object] | None = field(
+        default=None, compare=False, repr=False
+    )
@@ -37,6 +40,7 @@ class TriageProvider(Protocol):
         *,
         observation: RiskMatrixObservation | None = None,
         repair: RepairContext | None = None,
+        tool_call: ToolCall | None = None,
     ) -> ProviderStep:
diff --git a/backend/app/providers/mock.py b/backend/app/providers/mock.py
--- a/backend/app/providers/mock.py
+++ b/backend/app/providers/mock.py
@@ -14,6 +14,7 @@ class MockTriageProvider:
         *,
         observation: RiskMatrixObservation | None = None,
         repair: RepairContext | None = None,
+        tool_call: ToolCall | None = None,
     ) -> ProviderStep:
diff --git a/backend/app/providers/ollama.py b/backend/app/providers/ollama.py
--- a/backend/app/providers/ollama.py
+++ b/backend/app/providers/ollama.py
@@ -46,6 +46,7 @@ class OllamaTriageProvider:
         *,
         observation: RiskMatrixObservation | None = None,
         repair: RepairContext | None = None,
+        tool_call: ToolCall | None = None,
     ) -> ProviderStep:
diff --git a/backend/app/providers/router.py b/backend/app/providers/router.py
--- a/backend/app/providers/router.py
+++ b/backend/app/providers/router.py
@@ -3,7 +3,7 @@
 from collections.abc import Mapping
 
 from backend.app.schemas import RiskMatrixObservation, TriageRequest
 
-from .base import ProviderStep, RepairContext, TriageProvider
+from .base import ProviderStep, RepairContext, ToolCall, TriageProvider
 from .errors import ProviderConnectionError
@@ -20,6 +20,7 @@ class ProviderRouter:
         *,
         observation: RiskMatrixObservation | None = None,
         repair: RepairContext | None = None,
+        tool_call: ToolCall | None = None,
     ) -> ProviderStep:
@@ -27,4 +28,9 @@ class ProviderRouter:
             raise ProviderConnectionError(
                 "El proveedor solicitado todavía no está configurado."
             )
-        return provider.generate(request, observation=observation, repair=repair)
+        return provider.generate(
+            request,
+            observation=observation,
+            repair=repair,
+            tool_call=tool_call,
+        )
diff --git a/backend/app/services/triage_service.py b/backend/app/services/triage_service.py
--- a/backend/app/services/triage_service.py
+++ b/backend/app/services/triage_service.py
@@ -53,6 +53,7 @@ class TriageService:
     def triage(self, request: TriageRequest, *, request_id: str) -> TriageResult:
         observation: RiskMatrixObservation | None = None
         repair: RepairContext | None = None
+        tool_call: ToolCall | None = None
         repairs_used = 0
@@ -62,6 +63,7 @@ class TriageService:
                     request,
                     observation=observation,
                     repair=repair,
+                    tool_call=tool_call,
                 )
@@ -115,6 +117,7 @@ class TriageService:
                         "outcome": "accepted",
                     },
                 )
+                tool_call = candidate
                 repair = None
diff --git a/backend/app/prompts/messages.py b/backend/app/prompts/messages.py
--- a/backend/app/prompts/messages.py
+++ b/backend/app/prompts/messages.py
@@ -17,7 +17,7 @@ from .tool_context import RISK_MATRIX_TOOL, TOOL_SELECTION_PROMPT
 
 if TYPE_CHECKING:
-    from backend.app.providers.base import RepairContext
+    from backend.app.providers.base import RepairContext, ToolCall
@@ -113,6 +113,7 @@ def build_gemini_request(
     *,
     observation: RiskMatrixObservation | None,
     repair: RepairContext | None,
+    tool_call: ToolCall | None,
     temperature: float,
@@ -152,17 +153,8 @@ def build_gemini_request(
         }
         return body
 
-    arguments = observation.arguments.model_dump(mode="json")
-    final_parts: list[dict[str, object]] = [
-        {
-            "functionResponse": {
-                "name": observation.tool_name,
-                "response": {
-                    "result": observation.model_dump(mode="json"),
-                },
-            }
-        },
-        {"text": OUTPUT_FORMAT_PROMPT},
-    ]
+    if tool_call is None or tool_call.provider_context is None:
+        raise ValueError("Falta el contexto original de la llamada de herramienta.")
+    instructions = OUTPUT_FORMAT_PROMPT
     if repair is not None:
-        final_parts.append(
-            {
-                "text": (
+        instructions += (
+            "\n\n"
+            + (
                     "REPARACIÓN: corrige únicamente estos errores de contrato: "
                     + json.dumps(repair.validation_errors, ensure_ascii=False)
                     + "\nSALIDA RECHAZADA:\n"
                     + _serialize_output(repair.invalid_output)
-                )
-            }
         )
+    function_response: dict[str, object] = {
+        "name": observation.tool_name,
+        "response": {
+            "result": observation.model_dump(mode="json"),
+            "instructions": instructions,
+        },
+    }
+    original_parts = tool_call.provider_context.get("parts")
+    if isinstance(original_parts, list):
+        calls = [
+            call
+            for part in original_parts
+            if isinstance(part, Mapping)
+            and isinstance((call := part.get("functionCall")), Mapping)
+        ]
+        if len(calls) == 1 and isinstance((call_id := calls[0].get("id")), str):
+            function_response["id"] = call_id
     contents.extend(
         [
-            {
-                "role": "model",
-                "parts": [
-                    {
-                        "functionCall": {
-                            "name": observation.tool_name,
-                            "args": arguments,
-                        }
-                    }
-                ],
-            },
-            {"role": "user", "parts": final_parts},
+            dict(tool_call.provider_context),
+            {
+                "role": "user",
+                "parts": [{"functionResponse": function_response}],
+            },
         ]
diff --git a/backend/app/providers/gemini.py b/backend/app/providers/gemini.py
--- a/backend/app/providers/gemini.py
+++ b/backend/app/providers/gemini.py
@@ -96,16 +96,24 @@ class GeminiTriageProvider:
         *,
         observation: RiskMatrixObservation | None = None,
         repair: RepairContext | None = None,
+        tool_call: ToolCall | None = None,
     ) -> ProviderStep:
+        self._last_usage = ProviderUsage(None, None, None)
         if not self._api_key:
             raise ProviderConnectionError("No hay una clave externa configurada.")
         if not self._model:
             raise ProviderConnectionError("No hay un modelo externo configurado.")
+        if observation is not None and (
+            tool_call is None or tool_call.provider_context is None
+        ):
+            raise ProviderConnectionError(
+                "Falta el contexto de la llamada de herramienta de Gemini."
+            )
 
         body = build_gemini_request(
             request,
             observation=observation,
             repair=repair,
+            tool_call=tool_call,
             temperature=self._temperature,
@@ -113,9 +121,10 @@ class GeminiTriageProvider:
         )
         payload = self._post_generate_content(body)
-        parts = self._response_parts(payload)
-        tool_call = self._parse_tool_call(parts)
-        if tool_call is not None:
-            return tool_call
+        content = self._response_content(payload)
+        parts = self._response_parts(content)
+        parsed_tool_call = self._parse_tool_call(parts, content)
+        if parsed_tool_call is not None:
+            return parsed_tool_call
         return "".join(
@@ -300,16 +309,18 @@ class GeminiTriageProvider:
         )
 
     @staticmethod
-    def _response_parts(payload: Mapping[str, object]) -> list[Mapping[str, object]]:
+    def _response_content(payload: Mapping[str, object]) -> Mapping[str, object] | None:
         candidates = payload.get("candidates")
         if not isinstance(candidates, list) or not candidates:
-            return []
+            return None
         candidate = candidates[0]
         if not isinstance(candidate, Mapping):
-            return []
+            return None
         content = candidate.get("content")
-        if not isinstance(content, Mapping):
+        return content if isinstance(content, Mapping) else None
+
+    @staticmethod
+    def _response_parts(content: Mapping[str, object] | None) -> list[Mapping[str, object]]:
+        if content is None:
             return []
         parts = content.get("parts")
@@ -320,7 +331,10 @@ class GeminiTriageProvider:
         return [part for part in parts if isinstance(part, Mapping)]
 
     @staticmethod
-    def _parse_tool_call(parts: list[Mapping[str, object]]) -> ToolCall | None:
+    def _parse_tool_call(
+        parts: list[Mapping[str, object]],
+        content: Mapping[str, object] | None,
+    ) -> ToolCall | None:
         calls = [
@@ -335,4 +349,8 @@ class GeminiTriageProvider:
         if not isinstance(name, str) or not isinstance(arguments, Mapping):
             return None
-        return ToolCall(name=name, arguments=arguments)
+        return ToolCall(
+            name=name,
+            arguments=arguments,
+            provider_context=dict(content) if content is not None else None,
+        )
diff --git a/tests/unit/test_tool_orchestration.py b/tests/unit/test_tool_orchestration.py
--- a/tests/unit/test_tool_orchestration.py
+++ b/tests/unit/test_tool_orchestration.py
@@ -39,11 +39,13 @@ class SequenceProvider:
         self.steps = list(steps)
         self.observations = []
         self.repairs: list[RepairContext | None] = []
+        self.tool_calls = []
 
-    def generate(self, request, *, observation=None, repair=None):
+    def generate(self, request, *, observation=None, repair=None, tool_call=None):
         self.observations.append(observation)
         self.repairs.append(repair)
+        self.tool_calls.append(tool_call)
         return self.steps.pop(0)
diff --git a/tests/unit/test_triage_service.py b/tests/unit/test_triage_service.py
--- a/tests/unit/test_triage_service.py
+++ b/tests/unit/test_triage_service.py
@@ -36,11 +36,13 @@ class SequenceProvider:
         self.outputs = list(outputs)
         self.repairs: list[RepairContext | None] = []
         self.observations = []
+        self.tool_calls = []
 
-    def generate(self, request, *, observation=None, repair=None):
+    def generate(self, request, *, observation=None, repair=None, tool_call=None):
         self.observations.append(observation)
         self.repairs.append(repair)
+        self.tool_calls.append(tool_call)
         output = self.outputs.pop(0)
