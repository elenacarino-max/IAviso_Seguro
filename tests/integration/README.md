# integration

Se prueban `GET /health`, `POST /api/v1/triage`, validación 422, `request_id`, errores controlados 429/500/502/503, `Retry-After`, OpenAPI y continuidad del servidor tras un fallo.

Las pruebas utilizan el proveedor simulado y no realizan conexiones externas.
